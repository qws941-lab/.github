# Codex Triage
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _codex-triage.yml

name: Codex Triage

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  triage:
    uses: qws941/.github/.github/workflows/_codex-triage.yml@master
